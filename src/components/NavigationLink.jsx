const NavigationLink = ({ linkName }) => {
  return (
    <a href="#" className="text-lg px-[32px] py-[5px] hover:bg-white hover:text-[#B6CCDA] hover:rounded-full">
      {linkName}
    </a>
  );
};

export default NavigationLink;
